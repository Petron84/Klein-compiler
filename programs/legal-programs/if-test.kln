function main(n: integer, k: integer): integer
	print(n)
	if (n < k) then n else sub(n,k)

function sub(n: integer, k: integer): integer
	recurse(n-1,k)

function recurse(n: integer, k: integer): integer
	main(n,k)
